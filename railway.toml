[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate --no-input; python manage.py createcachetable; gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"